# Detección y tracking
ultralytics>=8.4
supervision>=0.27
opencv-python-headless>=4.8   # headless: sin dependencias de GUI (ideal para Jetson)
numpy>=1.24

# Servidor web (API REST + WebSocket + archivos estáticos)
flask>=3.0
flask-sock>=0.7

# Tracking con Re-ID — instalar solo con TRACKER_BACKEND="boxmot"
boxmot>=10.0

# Cámara Luxonis OAK-D W — instalar solo con CAMERA_BACKEND="oak"
# En Jetson: pip install depthai
depthai>=2.21